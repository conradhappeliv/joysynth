#include "Delay.h"

using namespace std;

Delay::Delay() {}

Delay::~Delay() {}

void Delay::subprocess(const RTArray<double>& input) {
//    processed.resize(25000, 0);
//    processed.insert(processed.begin(), input.begin(), input.end());
//    transform(processed.begin()+25000, processed.end(), input.begin(), processed.begin()+25000, plus<double>());
};